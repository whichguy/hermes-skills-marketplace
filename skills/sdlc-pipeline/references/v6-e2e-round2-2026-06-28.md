# v6 E2E Round 2 — 2026-06-28

## Context

After applying 6 fixes from Round 1 (git init, cwd, venv paths, debug extraction,
worktree enforcement, A1/A2), Round 2 validated the fixes with 3 new E2E tests
designed to stress specific paths.

## Test Design

| # | Test | Design Goal | Max Iter | Budget |
|---|---|---|---|---|
| 1 | Calculator (re-run) | Validate E402 fix — should no longer block | 3 | 300s |
| 2 | Binary Search | Force DEBUG→TEST tight loop (off-by-one traps) | 4 | 360s |
| 3 | JSON Parser | Complex project (nested objects, escape sequences) | 5 | 420s |

## Results

| # | Test | Tests | Final State | Status | Time | States |
|---|---|---|---|---|---|---|
| 1 | Calculator | 10/10 ✅ | COMPLETE | PASS | 69s | INIT→PLAN→IMPLEMENT→LINT_FIX→RUN_TESTS→VERIFYING→COMPLETE |
| 2 | Binary Search | 13/13 ✅ | COMPLETE | PASS | 133s | INIT→PLAN→IMPLEMENT→LINT_FIX→RUN_TESTS→DEBUG→RUN_TESTS→VERIFYING→COMPLETE |
| 3 | JSON Parser | 16/16 ✅ | COMPLETE | PASS | 139s | INIT→PLAN→IMPLEMENT→LINT_FIX→RUN_TESTS→VERIFYING→COMPLETE |

**Total: 39/39 tests passing, 341s, all COMPLETE.**

## Key Validations

### F1 Fix Confirmed: Terminal State in state_history
All 3 tests show `COMPLETE` as `state_history[-1]`. Before the fix, the terminal
state was never appended because the loop appends at the top of each iteration
and exits before re-entering. Fix: after the while loop, append `state.name` if
it differs from the last entry.

### DEBUG→TEST Tight Loop Validated
Test 2 (Binary Search) hit the debug path: the coder's first implementation had
bugs, the debugger fixed them, tests re-ran and passed (13/13). State history
shows the full loop: `IMPLEMENT → LINT_FIX → RUN_TESTS → DEBUG → RUN_TESTS → VERIFYING → COMPLETE`.

### E402 Fix Validated
Test 1 (Calculator re-run) completed with lint clean — E402 no longer blocks
the pipeline. The `--ignore E402` flag in `run_ruff()` works correctly.

### Complex Project Handling
Test 3 (JSON Parser) handled nested objects, escape sequences, arrays, and
all JSON primitives — 16 tests, all passing, no debug needed.

## Commits

| Commit | Fix |
|---|---|
| `f4e13e3` | Git init, cwd, venv paths, debug extraction, worktree enforcement, A1/A2 |
| `c7e15c5` | Negated GAPS detection (A1b) |
| `f7997b0` | Ruff E402 ignore |
| `b0c91aa` | F1: terminal state in state_history |

## Ad-Hoc Verification

28/28 checks passed: syntax (3 files), imports, F1 fix presence, all prior fixes
still present, dry-run E2E (F1 confirmed: COMPLETE in state_history), verdict
regression tests (4 cases including negated GAPS).
