<!--
model: glm-5.2:cloud
provider: ollama-glm
elapsed: 380.8s
chars: 11483
-->

┊ review diff
a/devops/sdlc-pipeline/references/v3.1-next-round-improvement-plan.md → b/devops/sdlc-pipeline/references/v3.1-next-round-improvement-plan.md
@@ -0,0 +1,265 @@
+# v3.1 Concurrent Dispatch — Next Round Improvement Plan
+
+> Generated: 2026-06-29
+> Status: DRAFT — awaiting review
+> Basis: Full source read of all 7 files + live test execution
+> Reviewer: GLM-5.2 cloud (this doc), pending Kimi + DeepSeek review
+
+---
+
+## CRITICAL FINDING: Tests Are Failing (Not 17/17 as Claimed)
+
+Before planning improvements, the most important discovery from reading the
+actual source code and running the tests:
+
+**The parallel E2E tests are NOT passing. Actual result: 2/7 pass, 5/7 ERROR.**
+
+```
+test_01_three_independent_tasks_merge_back    ERROR
+test_02_merge_conflict_same_file_aborts       ERROR
+test_03_failed_child_skipped_and_cleaned      ERROR
+test_04_worktree_leak_sweep_on_exception      ok    (passes by accident — expects RuntimeError)
+test_05_branch_deleted_after_merge            ERROR
+test_06_symlink_preservation                  ERROR
+test_07_patch_size_cap                        ok    (passes — fails before reaching create_worktree)
+```
+
+**Root cause:** `sdlc_parallel.py:273` calls `create_worktree(message=..., base_ref=..., repo_root=..., worktree_root=...)`, but `sdlc_worktree.py:55` defines `create_worktree(message: str, base_ref: str = 'main') -> str` — it does NOT accept `repo_root` or `worktree_root` parameters. Same mismatch on `remove_worktree(wt_path, repo_root=...)` at `sdlc_worktree.py:369` which only accepts `(worktree: str)`.
+
+The implementation was committed in `5cd29c2` with this mismatch. The two tests that "pass" do so by accident:
+- test_04 expects a RuntimeError from a mock — the signature TypeError gets wrapped in RuntimeError, so the test passes for the wrong reason.
+- test_07 fails at the 50MB patch cap check (line 225) before ever reaching `create_worktree()`.
+
+This means the merge-back logic, conflict resolution, branch deletion, symlink preservation, and failed-child cleanup paths have NEVER been exercised by tests. They are unverified production code.
+
+---
+
+## P0 — Blocking (Production Would Fail Immediately)
+
+### P0-1: Fix create_worktree/remove_worktree Signature Mismatch
+- **CATEGORY:** Correctness
+- **WHAT:** `sdlc_parallel.py` calls `create_worktree(repo_root=worktree, worktree_root=worktree_root)` and `remove_worktree(wt_path, repo_root=worktree)`, but `sdlc_worktree.py` doesn't accept those parameters. The functions use module-level constants `REPO_ROOT` and `WORKTREE_ROOT` from env vars instead.
+- **WHY:** Every call to `dispatch_parallel_isolated()` fails at the first `create_worktree()`. The state machine integration at `sdlc_state.py:1544` is dead code — it would crash on any multi-task plan. The "17/17 passing" claim in the context is incorrect.
+- **HOW:** Two options:
+  1. **(Preferred)** Update `sdlc_worktree.py` to accept optional `repo_root` and `worktree_root` parameters on `create_worktree()` and `remove_worktree()`, defaulting to the module-level constants. This allows `sdlc_parallel.py` to control worktree placement (nested inside parent) while preserving backward compatibility.
+  2. **(Minimal)** Remove the `repo_root=` and `worktree_root=` kwargs from `sdlc_parallel.py` calls and set `os.environ["SDLC_REPO_ROOT"]` and `os.environ["SDLC_WORKTREE_ROOT"]` before calling. Fragile — env vars are process-global and would break concurrent orchestrator instances.
+- **EFFORT:** S (1h) — 4 lines changed in `sdlc_worktree.py`, re-run tests to verify 7/7.
+
+### P0-2: Worktree Nested Placement — REPO_ROOT Mismatch
+- **CATEGORY:** Correctness
+- **WHAT:** Even after fixing the signature, `sdlc_parallel.py` sets `worktree_root = os.path.join(worktree, ".sdlc-parallel-wts")` and passes `repo_root=worktree`. But `create_worktree()` runs `git worktree add` with `cwd=REPO_ROOT` (the module constant, not the parameter). If `repo_root` is accepted as a parameter, the `cwd` must use it. Currently `create_worktree()` hardcodes `cwd=REPO_ROOT` at line 77.
+- **WHY:** In production, the orchestrator's worktree may not be `/opt/data` (the default `REPO_ROOT`). The state machine passes `worktree` (which could be a worktree path like `/opt/data/sdlc-worktrees/sdlc_my-task-abc123/`). `git worktree add` must run with `cwd` set to the actual repo root, not the stale module constant.
+- **HOW:** When adding `repo_root` parameter to `create_worktree()`, use it as `cwd` for the `git worktree add` command. Same for `remove_worktree()`. Verify with the E2E tests which set `SDLC_REPO_ROOT` to a temp dir.
+- **EFFORT:** S (1h) — part of P0-1 fix, just ensuring `cwd` uses the parameter.
+
+### P0-3: Re-verify All 7 Parallel E2E Tests Actually Pass
+- **CATEGORY:** Testing
+- **WHAT:** After fixing P0-1 and P0-2, run all 7 tests and confirm 7/7 pass. Then run them 3 consecutive times (as originally claimed). The merge-back logic (Phase D), conflict resolution, branch deletion order, and leak sweep have never been exercised.
+- **WHY:** 5 of 7 tests were ERROR-ing on the first `create_worktree()` call. The Phase D merge/rebase/cleanup code (lines 364-513 of `sdlc_parallel.py`) is unverified. We need to know if it actually works before planning improvements on top of it.
+- **HOW:** `python3 sdlc-parallel-e2e-test.py` with the signature fix applied. Fix any additional bugs that surface (likely: the `git apply --index` with `text=True` but `input=bytes` mismatch at line 304-308, the branch deletion order, the rebase conflict path).
+- **EFFORT:** M (half day) — fix P0-1/P0-2, then debug whatever else surfaces when the tests actually reach Phase D for the first time.
+
+---
+
+## P1 — Important (Production Readiness Gaps)
+
+### P1-1: Patch Apply Type Mismatch (text=True vs input=bytes)
+- **CATEGORY:** Correctness
+- **WHAT:** `sdlc_parallel.py:304-308` runs `subprocess.run(["git", "apply", "--index"], cwd=wt_path, input=patch_bytes, capture_output=True, text=True, timeout=30)`. The `text=True` flag makes subprocess expect `str` for `input`, but `patch_bytes` is `bytes` (read from file in binary mode at line 232). This will raise `TypeError: input must be str` when `patch_bytes` is non-empty.
+- **WHY:** This is in the Phase B3 code path that applies the orchestrator's untracked state to the child worktree. It runs whenever the orchestrator has uncommitted tracked changes. The test never reached this code because of the P0 signature mismatch.
+- **HOW:** Remove `text=True` from this specific `subprocess.run()` call (keep `capture_output=True`), OR decode `patch_bytes` to `str` with `patch_bytes.decode('utf-8', errors='surrogateescape')`. The former is safer for binary patches.
+- **EFFORT:** S (30 min)
+
+### P1-2: SIGINT/SIGTERM Cleanup Handler — Orphaned Worktrees on Kill
+- **CATEGORY:** Reliability
+- **WHAT:** If the orchestrator process is killed (Ctrl+C, OOM, SIGTERM) during Phase C (parallel dispatch) or Phase D (merge), child worktrees and branches are left orphaned. The `finally` block in `dispatch_parallel_isolated()` handles exceptions but NOT signals. A signal kills the process before `finally` runs.
+- **WHY:** In production, the user might Ctrl+C a long parallel dispatch. The result: 3 orphaned worktrees under `.sdlc-parallel-wts/`, 3 orphaned branches, and the orchestrator's working tree may be in a pre-merge checkpoint commit state. On next run, the empty-repo/detached-HEAD/mid-rebase checks at lines 183-208 would detect some of this, but orphaned worktrees are not checked.
+- **HOW:** Register a `signal.signal(signal.SIGINT, handler)` before Phase B that iterates `created_worktrees` and calls `remove_worktree()` on each. Restore the original handler after Phase D. Also register a `atexit` handler as a belt-and-suspenders approach. The handler should be idempotent (check if worktree still exists before removing).
+- **EFFORT:** M (3h) — signal handler registration, cleanup function, test with simulated SIGINT.
… omitted 187 diff line(s) across 1 additional file(s)/section(s)
I read all 7 source files and ran the actual E2E tests. Here is the critical finding and the plan.

**CRITICAL: The tests are NOT passing.** The context claims 17/17 green, but actual execution shows 5/7 ERROR, 2/7 pass (and the 2 that pass do so by accident — one expects a RuntimeError and gets a TypeError wrapped in RuntimeError; the other fails at the 50MB patch cap before reaching the broken code).

Root cause: `sdlc_parallel.py:273` calls `create_worktree(repo_root=..., worktree_root=...)` but `sdlc_worktree.py:55` defines `create_worktree(message, base_ref)` — no such parameters. Same for `remove_worktree(wt_path, repo_root=...)`. This mismatch was committed in `5cd29c2` and never caught. The entire Phase D merge-back logic (rebase, conflict resolution, branch deletion, leak sweep) has never been exercised by tests.

The full plan is at:
`/opt/data/skills/devops/sdlc-pipeline/references/v3.1-next-round-improvement-plan.md`

Summary of the plan, grouped by priority:

P0 — Blocking (3 items):
- P0-1: Fix create_worktree/remove_worktree signature mismatch (S, 1h)
- P0-2: Fix hardcoded `cwd=REPO_ROOT` in create_worktree — must use the repo_root parameter (S, 1h)
- P0-3: Re-verify all 7 tests actually pass after the fix, 3 consecutive runs (M, half day — expect more bugs in Phase D when tests reach it for the first time)

P1 — Important (7 items):
- P1-1: `subprocess.run(input=patch_bytes, text=True)` type mismatch — will TypeError when patch is non-empty (S, 30min)
- P1-2: SIGINT/SIGTERM cleanup handler — Ctrl+C orphans worktrees and branches (M, 3h)
- P1-3: No cancellation for hung children — `future.result()` timeout doesn't kill the subprocess, `pool.shutdown(wait=True)` blocks (L, 1+ day)
- P1-4: Merge-test gate is a commented-out placeholder — never runs (M, 4h)
- P1-5: No real model E2E test — all tests use mock dispatch_single, cwd propagation unverified (M, 4h)
- P1-6: parse_plan_tasks() fragility — brute-force find("[")/find("]") fails on nested arrays, and the PLAN prompt doesn't even request JSON output, so parallel dispatch likely never triggers in production (M, 3h)
- P1-7: Parallel dispatch only in PLAN state — DEBUG and VERIFYING could benefit (L, 1+ day)

P2 — Nice to Have (7 items):
- P2-1: Progress heartbeat during barrier (S, 2h)
- P2-2: Per-task wall-clock cap to remaining budget (S, 1h)
- P2-3: Structured logging for parallel dispatch events (M, 3h)
- P2-4: Chaos/fault injection tests for DeepSeek corner cases (L, 1+ day)
- P2-5: Agent-mode Ollama concurrency benchmark — 3.25x was raw-mode only (M, 3h)
- P2-6: Disk space check before worktree creation (S, 1h)
- P2-7: Phase 1.5 --resume uncertainty detection (M, 4h)

Recommended order: P0-1/P0-2/P0-3 first (everything else is built on sand until tests pass), then P1-1 (surfaces during P0-3), then P1-5 (real model smoke test), P1-6 (feature never triggers without it), P1-2 (Ctrl+C safety), P1-3 (hung child safety), P1-4 (merge-test gate).

Minimum viable set for production: P0-1+P0-2+P0-3, P1-1, P1-6, P1-5, P1-2. Approximately 1.5 days.

SUGGESTION:{"next": "Fix P0-1/P0-2 — update sdlc_worktree.py create_worktree/remove_worktree to accept repo_root and worktree_root params, then re-run the 7 E2E tests", "reason": "Tests are broken and Phase D merge-back logic has never been exercised — this is the foundation everything else depends on", "can_do": true}