# devloop — legacy SDLC retirement readiness (2026-06-30)

> **HISTORICAL SNAPSHOT** — describes the tree as of 2026-06-30. Superseded where it conflicts
> with SKILL.md: devloop IS now the sole wired SDLC path (legacy deleted 2026-07-01, commit
> 0344872), and COMPLETE **auto-merges** (the "NO auto-merge" statements below predate that
> decision).

Produced by the `devloop-retirement-readiness` workflow (4 Explore analysts + synthesis). The
conclusion that reshapes the plan: **devloop is NOT yet wired into the live build/debug path**, so
deleting the legacy engine today would SILENTLY DEGRADE production, not error.

## Bottom line

NOT ready. devloop is a deliberate KISS rewrite that is genuinely better at the kernel (DoD oracle, distinct-model judges, structural-coverage gate, blast-radius scoping, project outer loop) and proven in isolation — but it is a self-described v0.1 scaffold "Not wired into routing yet" (SKILL.md:27), devloop.enabled defaults false, and VERIFIED: zero .py files outside the devloop dir reference it. The live build/debug path is still routing.py -> pipeline.py -> sdlc.run_test_first_pipeline/debug_cascade. Deleting the legacy engine today would NOT raise an error — pipeline.py:160/173 guard dispatch with `and run_sdlc_build`/`and run_sdlc_debug`, so the imports silently become None and build_code/debug_code SILENTLY DEGRADE to single-shot dispatch with no autonomous loop. That silent-degradation is the decisive blocker. SHORTEST MUST-BUILD-FIRST LIST: (1) Repoint pipeline.py in all 3 trees from `from sdlc import run_test_first_pipeline/debug_cascade` to a call into devloop `runner.run_task` (map pipeline_mode test_first->build, debug_cascade->build with error context folded into the request), pick a worktree root under HERMES_WRITE_SAFE_ROOT=/opt/data, ensure HERMES_BIN=/opt/hermes/bin/hermes is present, and flip devloop.enabled=true; (2) close the council honesty seam — either wire gate.council_gate into loop.run_v1/stop_condition OR delete the "COUNCIL affirms" advertising from SKILL.md, because today council_affirm=True makes that gate vacuous (dead code, VERIFIED no runtime call site); (3) consciously accept the multi-model debug cascade, per-phase refusal/API-error retry, parallel multi-file code-gen, and impasse auto-diagnosis losses — all four fail SAFE to HUMAN_REVIEW rather than producing wrong output, so acceptance is defensible, but it lowers the hard-task auto-solve rate vs legacy. Recommended-before-canary (not strict blockers): flip HOLDOUT_ENABLED, resolve evidence_timeout_s TODO. Once (1) and (2) are done and verified on the container, the deletion_list is safe. Everything else legacy did (5 stagnation counters, diminishing-returns scoring, expanding-scope test tiers, two-pass tech-docs+simplify, STATUS.json polling) is glad-to-lose over-engineering.

## Parity gaps (legacy capability vs devloop)

| Verdict | Capability | Note |
|---|---|---|
| **blocker** | devloop wired into live routing (build_code/debug_code dispatch) | VERIFIED unwired: SKILL.md:27 'Not wired into routing yet', devloop.enabled default false, zero .py outside the devloop dir references it. routing.py still emits test_first/debug_cascade -> pipeline.py -> sdlc.py. Because pipeline.py:160/17 |
| **blocker** | devloop final COUNCIL gate is honest (not dead code) | VERIFIED: gate.council_gate (gate.py:56) has NO runtime call site (only tests + SKILL.md/refs). The wired path threads council_affirm=True hardcoded (loop.py:159, runner.py:23) into stop_condition (gate.py:145), making the 'council affirmed |
| **nice-to-have** | Multi-model DEBUG cascade (model escalation on failure: legacy qwen->kimi) | VERIFIED absent: loop.py re-runs the SAME _do_implement coder on red (lines 122/217), no escalation. Legacy debug_cascade (sdlc.py:866) escalated to a stronger model. Devloop fails SAFE — exhausts rebuild/replan and routes to HUMAN_REVIEW r |
| **drop-ok** | Final multi-model council P0/P1 improvement-and-refine loop (legacy council_review feeding REFINE) | Legacy fanned a review to 3 models, parsed P0/P1, and fed them back through another SDLC pass (gold-plating after the bar is met). Dropping post-DoD-SATISFIED polishing aligns with devloop's stop-at-the-bar philosophy. The improvement-itera |
| **drop-ok** | Parallel multi-task code-gen with per-task git-worktree isolation + rebase/merge-back | Legacy sdlc_parallel.dispatch_parallel_isolated decomposed plans into file-disjoint tasks run concurrently then merged back with conflict handling. Devloop is sequential; blast_radius.scope routes too-big work to HUMAN_REVIEW (the SAFETY ex |
| **nice-to-have** | Per-phase output quality gates + retry (refusal / API-error / too-short / unstructured) | Legacy dispatch_with_evaluation retried each phase on REFUSAL_PATTERNS/API-error/length. Devloop relies on fail-closed parsing (empty DoD -> HUMAN_REVIEW) + evidence exit codes; a refusing model is caught only indirectly via no-file-change  |
| **nice-to-have** | Impasse root-cause auto-diagnosis on FAILED (DeepSeek proposes PROJECT.md edits) | Legacy _emit_iterative_summary (sdlc_state.py:2045) dispatched a model to diagnose the structural blocker. Devloop on HUMAN_REVIEW emits the gate reason + a prose kanban proposal, partially mitigated by LESSONS journal + project report. Imp |
| **drop-ok** | Two-pass tech-docs + code-simplify with test-revert safety net | Legacy ran tech_docs->simplify_code(reverts if it breaks tests)->tech_docs. Devloop folds handoff-docs + targeted error-checking into the coder prompt by construction (_IMPL_STYLE). Reasonable KISS drop; the dedicated consolidation pass wit |
| **drop-ok** | Expanding-scope test tiers (unit->integration->E2E, fail-fast) | Legacy TEST_SUITES ran progressively expensive suites. Devloop generates tests per DoD criterion (arguably better, acceptance-anchored). Explicit integration/E2E scope now depends on the DoD criteria capturing them — acceptable model shift, |
| **drop-ok** | Five stagnation counters + diminishing-returns scoring + code-enforced repeated-root-cause | Explicitly replaced (config.py:38 comment) by one backstop: backoff_exhausted over MAX_LOCAL_REBUILDS=3/MAX_REPLANS=3 + the DoD-SATISFIED stop. Documented design choice. Minor weakening: the 'same root cause N times' code detector becomes o |
| **drop-ok** | External STATUS.json progress polling for long runs | Legacy save_state wrote STATUS.json for external pollers. Devloop observability is the per-run trace.jsonl rendered by render.py/trace_view.py — equivalent enough. |
| **drop-ok** | Lint with autopep8 fallback + revert-on-broken-fix | Near-parity: devloop lint.py is an in-phase _lint_gate that discovers/skips linters and hands per-file failures back to the coder. Legacy's autopep8 fallback + revert-on-break is slightly richer but the gating behavior is comparable. |
| **nice-to-have** | Holdout anti-gaming test split | NEW vs legacy (not a parity gap) but scaffolded OFF: dod_oracle.split_holdout exists, config.HOLDOUT_ENABLED=False. Flip before canary to harden against the coder gaming visible tests; not required to reach legacy parity. |

## Deletion list (safe ONLY after the 2 blockers are closed)

- `/Users/dadleet/.hermes/skills/productivity/ask/scripts/sdlc.py`
- `/Users/dadleet/.hermes/skills/productivity/ask/scripts/sdlc_state.py`
- `/Users/dadleet/.hermes/skills/productivity/ask/scripts/sdlc_parallel.py`
- `/Users/dadleet/.hermes/skills/productivity/ask/scripts/sdlc_worktree.py`
- `/Users/dadleet/.hermes/skills/productivity/ask/tests/test_sdlc.py`
- `/Users/dadleet/.hermes/skills/devops/sdlc-pipeline/references/sdlc-parallel-e2e-test.py`
- `/Users/dadleet/.hermes/profiles/worker/skills/productivity/ask/scripts/sdlc.py`
- `/Users/dadleet/.hermes/profiles/worker/skills/productivity/ask/scripts/sdlc_state.py`
- `/Users/dadleet/.hermes/profiles/worker/skills/productivity/ask/scripts/sdlc_parallel.py`
- `/Users/dadleet/.hermes/profiles/worker/skills/productivity/ask/scripts/sdlc_worktree.py`
- `/Users/dadleet/.hermes/profiles/worker/skills/productivity/ask/tests/test_sdlc.py`
- `/Users/dadleet/.hermes/profiles/worker/skills/devops/sdlc-pipeline/references/sdlc-parallel-e2e-test.py`
- `/Users/dadleet/.hermes/profiles/reviewer/skills/productivity/ask/scripts/sdlc.py`
- `/Users/dadleet/.hermes/profiles/reviewer/skills/productivity/ask/scripts/sdlc_state.py`
- `/Users/dadleet/.hermes/profiles/reviewer/skills/productivity/ask/scripts/sdlc_parallel.py`
- `/Users/dadleet/.hermes/profiles/reviewer/skills/productivity/ask/scripts/sdlc_worktree.py`
- `/Users/dadleet/.hermes/profiles/reviewer/skills/productivity/ask/tests/test_sdlc.py`
- `/Users/dadleet/.hermes/profiles/reviewer/skills/devops/sdlc-pipeline/references/sdlc-parallel-e2e-test.py`
- `/Users/dadleet/.hermes/hermes-verify-sdlc-patches.py`
- `/Users/dadleet/.hermes/scripts/kanban-sdlc.sh`
- `/Users/dadleet/.hermes/tmp/hermes-verify-sdlc-v5-design.py`
- `/Users/dadleet/.hermes/tmp/verify_sdlc_lint_fixes.py`

## Repoint map

- **skills/productivity/ask/scripts/pipeline.py (+ profiles/worker + profiles/reviewer copies)** — _HARD (module import + in-process call) — the only live runtime seam_  
  LIVE PRODUCTION SEAM. Replace `from sdlc import run_test_first_pipeline as run_sdlc_build` / `debug_cascade as run_sdlc_debug` (lines 55-56) and the dispatch at lines 160/173 with a call into devloop runner.run_task. Map pipeline_mode=='test_first'->build, 'debug_cascade'->build with code/error folded into the request string. Translate run_task's {result.terminal in COMPLETE/HUMAN_REVIEW/NO_TERMINATION, worktree, charter, trace_path} back into pipeline.py's expected result dict. MUST do this BEFORE deleting sdlc.py or build_code/debug_code silently degrade to single dispatch.
- **skills/productivity/ask/scripts/routing.py (x3 trees)** — _SOFT (string config consumed by pipeline.py)_  
  Decision strings 'pipeline':'test_first' (line 75) and 'debug_cascade' (line 83) decide the mode. Keep them or rename, but ensure pipeline.py now maps these to the devloop path. No import of the engine here — pure config string.
- **skills/productivity/ask/tests/test_pipeline.py, test_pipeline_e2e.py, test_contract.py (x3 trees)** — _MEDIUM (test patches + symbol references)_  
  These exercise run_test_first_pipeline/debug_cascade/read_learnings_formatted. Rewrite the sdlc-specific cases against the devloop runner path (or delete them); they patch pipeline.run_sdlc_build/run_sdlc_debug, which disappear. test_sdlc.py is engine-only -> delete (listed in deletion_list).
- **skills/productivity/ask/scripts/verify_bugs.py + /Users/dadleet/.hermes/hermes-verify-sdlc-patches.py** — _TEXT-read only (not importers) — no runtime break_  
  Open sdlc.py/sdlc_state.py as TEXT for patch verification and hardcode /opt/data paths (already broken on this host). Retire (hermes-verify-sdlc-patches.py is in deletion_list); strip sdlc references from verify_bugs.py.
- **skills/devops/kanban-orchestrator (SKILL.md + references/)** — _PROSE/doc only_  
  Pure-Markdown, no scripts. Edit prose: SKILL.md:130-134 'load the sdlc-pipeline skill' -> point at devloop; SKILL.md:435 + references/sdlc-skills-alignment.md:46-49 + references/sdlc-live-test-results.md + references/kanban-sdlc-validation-plan.md reference kanban-sdlc.sh (being deleted) -> remove/repoint. No code path breaks; risk is a worker trying to load a missing skill or run a missing script.
- **skills/devops/sdlc-pipeline (SKILL.md + references/)** — _PROSE/doc only_  
  Decomposition playbook, NOT a direct engine caller (engine appears only in references/ docs). Repoint its references to devloop or retire the skill. references/sdlc-parallel-e2e-test.py is dead engine test -> delete (in deletion_list).
- **skills/productivity/multi-model-code-review** — _NONE (anecdotal text)_  
  No change. Its 4 SDLC mentions are historical review-target anecdotes; zero functional dependency.
- **skills/software-development/devloop/SKILL.md + config** — _Self (the replacement) — wiring + honesty fix_  
  Flip Status off 'Not wired'; set devloop.enabled=true; and EITHER wire gate.council_gate into loop.run_v1/stop_condition OR remove the advertised enforced COUNCIL so docs match runtime.

## Integration contract (how repointed consumers call devloop)

There is NO CLI or slash-command today — repointed consumers must call the Python functions directly (SKILL.md names hermes kanban + hermes cron as the INTENDED future entrypoint, not yet built). SINGLE TASK (build/debug): runner.run_task(repo, request, root, name, *, judge_a=None, judge_b=None, council_affirm=True, make_*=None) where repo=source git repo path, request=the fuzzy free-form message string (for debug, fold code+error_feedback into request), root=worktree root which MUST live under HERMES_WRITE_SAFE_ROOT=/opt/data, name=run/branch id. Returns {result:{terminal in COMPLETE|HUMAN_REVIEW|NO_TERMINATION, state, trace_path=<run_dir>/trace.jsonl, reason}, worktree:{path, branch='devloop/<name>', repo}, charter:{...}}. NO auto-merge — on COMPLETE the branch is left for human review; pipeline.py must surface the branch, not assume merged. If the ambiguity gate doesn't PROCEED it returns early with terminal=='HUMAN_REVIEW'. BACKLOG/PROJECT: project.run_project(repo, root, purposes:list[str], *, project_dir=None, max_attempts=3, **run_task_kwargs) -> {plan_path, lessons_path, achieved, blocked, items, report:<markdown>} (the polished human report exists only on run_project, not run_task). ENV PRECONDITIONS for real runs: HERMES_BIN=/opt/hermes/bin/hermes must exist (every model call shells out via `hermes chat -q <prompt> -m <model> -Q --yolo`), file tools only write under /opt/data, and dispatch.assert_distinct_models(CODER,DESIGNER,JUDGE_A,JUDGE_B) is enforced at run_task start. Off-container callers must inject make_charter/make_refiner/make_advisor/make_designer/make_implementer + judges (the deterministic/test path) or set HERMES_BIN + the DEVLOOP_* roster env vars. NOTE: the wired loop is the narrow v1 (CHARTER->ambiguity->design tests from DoD->coverage gate->[implement->lint gate->evidence per criterion->judge]); council/holdout/debug-cascade in SKILL.md prose are scaffold/stubbed — repoint only against what is actually wired.

## Risks

- SILENT DEGRADATION (top risk): pipeline.py dispatch is guarded by `and run_sdlc_build`/`and run_sdlc_debug` (lines 160/173). If sdlc.py is deleted before pipeline.py is repointed, the import becomes None and build_code/debug_code quietly fall through to single-shot dispatch — no error, no autonomous loop, hard to notice in production.
- TRIPLICATION: all four engine files + tests + sdlc-pipeline references exist in main, profiles/worker, and profiles/reviewer trees (VERIFIED). A delete/repoint that misses a profile copy leaves stale engine code that profile-synced agents still load.
- ENV MISMATCH: devloop hardcodes HERMES_BIN=/opt/hermes/bin/hermes and write-safe root /opt/data; legacy hardcodes /opt/data/sdlc-worktrees. Neither resolves on this macOS host (/Users/dadleet/.hermes). Wiring + end-to-end verification must happen on the container, not locally, or runs no-op/fail.
- COUNCIL FALSE-ADVERTISING: SKILL.md presents the final COUNCIL as an enforced gate, but it is dead code (council_affirm=True hardcoded). Operators trusting the docs would believe completion requires council affirmation when it does not.
- SOLVE-RATE REGRESSION: no debug-cascade and no per-phase refusal/API-error retry means hard tasks legacy auto-escalated now route to HUMAN_REVIEW. Confirm the increased human-handoff volume is acceptable before deletion.
- THROUGHPUT LOSS: parallel multi-file code-gen is gone and its kanban auto-decomposition replacement is prose-only/unwired — large multi-file work serializes or stalls at HUMAN_REVIEW.
- DON'T-DELETE-THE-WRONG-THING: two legacy orchestrators coexist — v5 sdlc.run_test_first_pipeline (what pipeline.py actually calls) and v6 sdlc_state.run_iterative_state_machine (only its own CLI/E2E). Census found NO live cron/shell invoking v6 or kanban-sdlc.sh, but re-confirm no operator/launchd job invokes them before deleting.
- DEVLOOP v0.1 SEAMS: HOLDOUT_ENABLED=False, evidence_timeout_s TODO, dod_oracle.generate_tests_from_criteria raises NotImplementedError (on a path the wired designer bypasses). The runtime contract is narrower than SKILL.md prose — repoint only against wired behavior.
- verify_bugs.py / hermes-verify-sdlc-patches.py open the engine files as TEXT and hardcode /opt/data/skills paths; they won't break at import but will silently no-op once their targets are gone.

## Verification checklist (post-retirement)

- [ ] After repoint, run `find /Users/dadleet/.hermes -name '*.py' | xargs grep -l 'from sdlc\|import sdlc\|run_test_first_pipeline\|run_iterative_state_machine\|debug_cascade\|dispatch_parallel\|sdlc_worktree'` and confirm ZERO live importers remain across all 3 trees (only deleted/retired files should have matched before).
- [ ] Confirm devloop.enabled=true and that a build_code request actually dispatches devloop runner.run_task — assert a devloop trace.jsonl + a 'devloop/<name>' worktree branch are produced (run on the container where HERMES_BIN exists), not the old sdlc path.
- [ ] Confirm pipeline.py no longer references run_sdlc_build/run_sdlc_debug and that test_pipeline.py / test_pipeline_e2e.py / test_contract.py pass against the devloop path (or have been rewritten/removed).
- [ ] Council honesty: grep shows either a real runtime call site for gate.council_gate inside loop.run_v1/stop_condition, OR SKILL.md no longer advertises an enforced COUNCIL gate. No middle state.
- [ ] Run the full ask-skill test suite green after deleting test_sdlc.py and the sdlc-parallel-e2e-test.py references; no ImportError from dangling sdlc symbols.
- [ ] Run devloop's own suite (per memory: 112 tests + 26/26 mutants) green after the wiring/de-advertising edits.
- [ ] Grep kanban-orchestrator + sdlc-pipeline prose for 'sdlc-pipeline' skill-load directives and 'kanban-sdlc.sh' references and confirm none instruct loading a deleted skill or running a deleted script.
- [ ] Confirm no cron/launchd/operator script invokes sdlc.py, sdlc_state.py, or kanban-sdlc.sh (re-check the container's scheduler, not just the repo).
- [ ] Profile-sync parity: diff worker + reviewer trees against main and confirm no stale engine copies survive the deletion.
- [ ] End-to-end smoke: a debug_code request (code+error folded into the request) returns a terminal in {COMPLETE, HUMAN_REVIEW} with a left-for-review worktree branch — confirm pipeline.py surfaces the branch (no auto-merge assumed).

## Analyst findings (summaries)

### Legacy SDLC engine — public surface & entrypoints (ask/scripts + sdlc-pipeline orchestrator)

The "legacy SDLC engine" is four Python modules under /Users/dadleet/.hermes/skills/productivity/ask/scripts/: sdlc.py (v5 linear test-first pipeline + per-phase model dispatch + CLI), sdlc_state.py (v6 iterative state machine — the current orchestrator, with its own CLI), sdlc_parallel.py (parallel dispatch w/ worktree isolation), and sdlc_worktree.py (git worktree lifecycle + learning commits). There are TWO distinct runtime entrypoints: (a) the live in-process path where ask/scripts/pipeline.py imports run_test_first_pipeline (as run_sdlc_build) and debug_cascade (as run_sdlc_debug) from sdlc and dispatches them when routing.py sets pipeline_mode='test_first'/'debug_cascade'; and (b) two standalone argparse CLIs (`python3 sdlc.py <message> --mode build|debug` and `python3 sdlc_state.py --message --worktree`). The sdlc-pipeline SKILL.md is NOT a direct caller of this engine — it is a Kanban decomposition playbook that kicks off SDLC work by creating `hermes kanban create` task chains; the engine code only appears in its references/ docs. The NEW replacement is the `devloop` skill (software-development/devloop), whose worktree.py is explicitly "was sdlc_worktree.py" (a deliberate rewrite, not a port) and whose DECISIONS.md tallies the ~4,917 legacy LOC it supersedes — so the legacy engine is what must be repointed/deleted, and only the legacy files (plus pipeline.py + tests) reference it.

### Coupling of kanban-orchestrator and multi-model-code-review skills to the legacy SDLC engine

Neither skill is code-coupled to any SDLC engine. Both directories ship only Markdown (a SKILL.md plus references/*.md) — there are no .py or .sh files in either, so there is no import, subprocess call, or CLI invocation of the SDLC engine code (the ask-skill Python engine sdlc.py/sdlc_state.py/sdlc_parallel.py/sdlc_worktree.py, the kanban-sdlc.sh script, or the sdlc-pipeline skill). kanban-orchestrator is LOOSELY coupled: it mentions SDLC heavily in prose (48 hits) and contains soft, human/agent-directed references — a "load the sdlc-pipeline skill" instruction and pointers to the kanban-sdlc.sh script — but it implements its own "SDLC chain" pattern via the hermes kanban CLI, not by calling the engine. multi-model-code-review has essentially NO coupling: its 4 SDLC mentions are all historical case-study anecdotes (the SDLC code was merely the review TARGET in past sessions), with zero functional dependency. On deletion, no code path in either skill breaks; the only fallout is dangling documentation references, almost entirely inside kanban-orchestrator.

### devloop public entrypoint + invocation contract (what replaces the legacy v5/v6 SDLC engine)

devloop currently exposes TWO public Python entrypoints, not a CLI or wired skill command: runner.run_task(...) for a single fuzzy request, and project.run_project(...) for a backlog of "purposes" (the outer drain loop that calls run_task per item). The input contract is a request STRING (single task) or a LIST of purpose strings (project); the output is a structured dict carrying a terminal verdict, the git worktree handle, the Charter, a per-task JSONL trace, and (for projects) a human-readable markdown report. There is NO auto-merge — on COMPLETE the worktree branch is left for review. Models are dispatched by dispatch.py by shelling out to the Hermes binary (hermes chat -q ... -m <model> -Q --yolo), mirroring the ask skill, with a 6-role env-overridable roster. SKILL.md explicitly marks this as a v0.1 scaffold "Not wired into routing yet" (config devloop.enabled default false), and names hermes kanban / hermes cron as the INTENDED future run entrypoint — so repointed consumers must today call the Python functions directly, not a slash-command or CLI.

### Legacy SDLC engine vs devloop — capability parity audit (what legacy provides that devloop does not)

Devloop is a deliberate KISS rewrite that REPLACES legacy's ~4,900 LOC v5 linear pipeline + v6 iterative state machine with a small DoD-as-oracle trust kernel, and it ADDS genuinely new capabilities legacy lacked (per-criterion DoD oracle, independent assertion judges, structural coverage gate, blast-radius scoping, a multi-purpose project outer loop, Phase-0 advisors intent review, async HUMAN_REVIEW/kanban prose). Most legacy machinery (5 stagnation counters, diminishing-returns scoring, expanding-scope unit/integration/E2E suites, per-phase refusal/quality-retry, the post-build P0/P1 improvement-and-refine council, the two-pass tech-docs+simplify subsystem) is consciously dropped in favor of evidence/exit-code truth and the single DoD-SATISFIED stop — these are defensible YAGNI drops. BUT three things are load-bearing risks, not clean drops: (1) the final COUNCIL gate that SKILL.md advertises is DEAD CODE — gate.council_gate has zero call sites and the wired loop hardcodes council_affirm=True, so the "council affirms" leg of stop_condition is vacuous; (2) the multi-model DEBUG cascade (legacy qwen→kimi; SKILL.md's Kimi→Qwen→DeepSeek) is NOT wired — the loop just re-runs the single coder with failure context; (3) parallel multi-task code-gen with git-worktree isolation + automatic merge-back is entirely absent. The overarching completion-blocker: SKILL.md explicitly states devloop is a v0.1 scaffold "not wired into routing yet," so deleting legacy today loses ALL function until devloop is wired and its hardcoded seams (council, holdout) are closed.</summary>
<parameter name="capabilities">["CAP: Final multi-model COUNCIL review at completion — [GAP] — Legacy council_review (sdlc.py:1193) fans a review prompt to 3 models in parallel (COUNCIL_PANEL, sdlc.py:1186), parses P0/P1 items (extract_improvement_items, sdlc.py:1380), and FEEDS THEM BACK through the SDLC for refinement. Devloop SKILL.md advertises a 'COUNCIL DoD-check (advisors skill, via gate.council_gate)' as a core gate, but gate.council_gate (gate.py:56) has ZERO call sites and the wired path (loop.run_v1, loop.py:158; runner.run_task, runner.py:23) passes council_affirm=True hardcoded. So stop_condition's council_affirmed leg (gate.py:145) is vacuously true. This is the single most dangerous wrongly-claimed parity: the council looks enforced in prose but is dead code in the runtime.", "CAP: Council P0/P1 improvement-and-refine loop — [dropped-ok, but note] — Even once council_gate is wired, devloop's council is a binary affirm/deny completeness gate, NOT legacy's 'identify P0/P1 issues then refine the code through another SDLC pass' (sdlc.py REVIEW->REFINE->IMPLEMENT). Dropping gold-plating after DoD-SATISFIED aligns with devloop's stop-at-the-bar philosophy, so acceptable — but the improvement-iteration capability itself is genuinely gone.", "CAP: Multi-model DEBUG cascade (model escalation on failure) — [GAP] — Legacy debug_cascade (sdlc.py:866) escalates qwen-coder->kimi (DEBUGGER_CASCADE, sdlc.py:860), feeding each failure forward to a stronger model. SKILL.md claims 'DEBUG cascade in-place (Kimi->Qwen->DeepSeek via ask)'. But the wired loop.run_v1 has no debug module: on red evidence it just sets last_failure and re-calls the SAME single coder (implementer_via_ask, dispatch.py:249) via _do_implement. No model escalation exists in code — both vs legacy and vs devloop's own spec.", "CAP: Parallel multi-task code-gen with git-worktree isolation + merge-back — [GAP] — Legacy sdlc_parallel.dispatch_parallel_isolated (sdlc_parallel.py:148) decomposes a plan into independent file-disjoint tasks (parse_plan_tasks, sdlc_state.py:183), runs them concurrently each in its own worktree, then rebases/merges back with conflict handling and feeds merge failures into the next plan. Devloop's only concurrency is judge calls (dod_oracle.py:68 ThreadPoolExecutor); BUILD is sequential and project.run_project (project.py:113) drains purposes one-at-a-time. Devloop instead routes too-big blast radius to HUMAN_REVIEW + a hermes-kanban decomposition (prose). Throughput/parallelism capability is lost; arguably an acceptable correctness-biased drop, but it IS a real lost capability.", "CAP: Two-pass tech-docs + code-simplification subsystem — [dropped-ok] — Legacy runs tech_docs pass-1 (sdlc.py:1047) -> simplify_code (sdlc.py:1105, re-verifies & REVERTS if simplification breaks tests) -> tech_docs pass-2, driven by DOCSTRING_DIRECTIVE (sdlc.py:1019). Devloop has none of these (grep: zero matches for tech_docs/simplify/docstring in runtime). It folds handoff-docs + targeted error-checking into the coder prompt by construction (_IMPL_STYLE, dispatch.py:238). Reasonable KISS drop, but the dedicated consolidation pass WITH a test-revert safety net is genuinely gone.", "CAP: Expanding-scope test tiers (unit -> integration -> E2E, fail-fast) — [dropped-ok] — Legacy TEST_SUITES (sdlc.py:442) + run_test_suites (sdlc.py:808) run progressively expensive suites and stop on first failure. Devloop generates tests per DoD criterion (designer_via_ask, dispatch.py:273; testgen.collect_test_map) anchored to acceptance criteria instead. Different (arguably better, DoD-anchored) model; explicit integration/E2E scope levels now depend entirely on the DoD criteria capturing them.", "CAP: Per-phase output quality gates + retry (refusal/API-error/length/structure) — [dropped-ok] — Legacy dispatch_with_evaluation (sdlc.py:259) retries each phase up to MAX_PHASE_ROUNDS with evaluators detecting REFUSAL_PATTERNS (sdlc.py:110), API-error strings, too-short/unstructured output. Devloop has no per-dispatch quality evaluator; it relies on fail-closed parsing (charter_via_ask -> empty DoD -> HUMAN_REVIEW, dispatch.py:173) and evidence exit codes. Stronger where it matters, but explicit refusal/API-error detection is not present (a refusing model is caught only indirectly via no-file-change -> dispatch_error).", "CAP: Five stagnation counters + diminishing-returns scoring — [dropped-ok] — Legacy tracks test/root_cause/gap/cascade stagnation + uncertain_streak (SDLCRun, sdlc_state.py:140-152), repeated_root_cause over LEARNINGS (sdlc_state.py:958), and DiminishingReturnsTracker (sdlc_state.py:251, score-delta + repeated-feedback). Devloop deliberately replaces ALL of these (config.py:38 comment; SKILL.md) with one code-enforced backstop: backoff_exhausted (gate.py:151) over rebuild_count/replan_count, plus the DoD-SATISFIED stop. Explicit, documented design choice — acceptable. Caveat: the 'same root cause N times' code-enforced detector becomes only a prose back-off-table heuristic ('same failure 2x -> suspect wrong test'), i.e. weaker.", "CAP: Impasse root-cause diagnosis on FAILED — [GAP, minor] — On terminal FAILED, legacy dispatches DeepSeek to diagnose the structural blocker and propose specific PROJECT.md edits (_emit_iterative_summary, sdlc_state.py:2045). Devloop on HUMAN_REVIEW emits only the gate reason + (prose) a kanban decomposition proposal; no model-driven impasse diagnosis. Partially mitigated by the LESSONS journal + project report (project.py:_render_report), but the auto-diagnosis capability is lost.", "CAP: External progress file for long runs — [dropped-ok] — Legacy save_state writes STATUS.json alongside the checkpoint (sdlc_state.py:764) so external tools can poll progress. Devloop has no STATUS.json; observability is the per-run trace.jsonl (loop.py:_emit) rendered by render.py/trace_view.py. Equivalent-enough.", "CAP: Lint with aggressive auto-fix + revert-on-break — [devloop-has (≈parity)] — Legacy lint_code (sdlc.py:1905) runs ruff+autopep8+pyflakes+mypy, auto-fixes, and reverts if a fix breaks syntax; run_all_linters (sdlc_state.py:1118) in the loop. Devloop lint.py is an in-phase gate (loop._lint_gate) that discovers runnable linters, skips missing ones, and hands per-file failures back to the coder for rebuild. Comparable; legacy's autopep8 fallback + revert-on-break is slightly richer.", "CAP: Checkpoint / resume — [devloop-has] — Legacy ITERATION_STATE.json + MAX_RESUMES (sdlc_state.py:1370). Devloop state.save_checkpoint/load_checkpoint (state.py:89-112) with atomic write-temp-rename and fail-safe-on-corrupt; backoff counters survive resume. Parity (devloop's atomicity is cleaner).", "CAP: LEARNINGS journal — [devloop-has (≈parity, legacy richer)] — Both append/read a JSONL journal. Legacy adds recurring-root-cause Counter formatting (read_learnings_formatted, sdlc_state.py:865) + repeated_root_cause stagnation. Devloop state.read_learnings/append_learning (state.py:136-169) is simpler and feeds lessons into both per-task back-off and the project outer loop.", "CAP: Per-criterion DoD oracle (coverage + assertion judges) — [devloop-has, NEW] — dod_oracle.check_structural_coverage + judge_assertions (dod_oracle.py:25,35), testgen real-collected map (testgen.collect_test_map), distinct judge!=coder!=designer enforcement (dispatch.assert_distinct_models, dispatch.py:361). Legacy has NO equivalent. Net capability GAIN, not a loss.", "CAP: Phase-0 advisors intent review — [devloop-has, NEW] — advisor_via_ask (dispatch.py:214) reviews the refined DoD for completeness pre-build and folds blocking gaps into open_questions. Legacy had no pre-build intent council. GAIN.", "CAP: Multi-purpose project OUTER loop — [devloop-has, NEW] — project.run_project (project.py:113) drains a master-TODO of purposes with bounded re-attempts, lessons carry-over, and fail-closed escalate/reattempt classification. Legacy operated on a single message. GAIN.", "CAP: Async HUMAN_REVIEW (Telegram/cron) + kanban entrypoint — [devloop-has (prose), NEW] — Grep shows no telegram/cron/kanban in either codebase's Python; SKILL.md describes them as native-loop-orchestrated prose. Legacy only printed options to stderr (sdlc_state.py:2111). Not a legacy capability, so not a gap — devloop ADDS it.", "CAP: Holdout anti-gaming test split — [devloop-has but OFF] — dod_oracle.split_holdout (dod_oracle.py:95) exists but config.HOLDOUT_ENABLED=False (config.py:35). Scaffolded seam, must be flipped before canary. NEW vs legacy; flagged as an unclosed seam.", "CAP: META — devloop is an unwired v0.1 scaffold — [GAP / completion-blocker] — SKILL.md line 27: 'Status: v0.1 scaffold (migration step 0/1). Not wired into routing yet.' Plus hardcoded seams: council_affirm=True (loop.py:159), HOLDOUT_ENABLED=False, evidence_timeout_s TODO (evidence.py), generate_tests_from_criteria raises NotImplementedError (dod_oracle.py:124, though the wired designer path bypasses it). Deleting legacy now would lose ALL function until devloop is routed and these seams are closed."]
